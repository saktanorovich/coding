import utils.io.*;
import java.io.*;

// Problem ?
public class Template {
    public void process(int testCase, InputReader in, PrintWriter out) {
        out.format("Case #%d: %d\n", testCase, in.nextInt());
    }
}
